import { Container } from '@mui/material';
import AppBarNav from './AppBar';

function App() {
  return (
    <Container fixed sx={{ mb: '15px' }}>
      <AppBarNav/>
    </Container>
  );
}

export default App;
